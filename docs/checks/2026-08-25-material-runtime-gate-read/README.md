# Material Runtime Gate Dependency Read

The experiment returned `NOT_RUN_PREREQUISITE_FAILED` before construction or
model contact. `E-MATERIAL-SEMANTIC-ADMISSION` found no reference-unread
Earnings-22 meetings, so the required three-meeting development and
three-meeting confirmation split could not be frozen.

No threshold was fit, no Pass0 was inspected, no official material was
downloaded, and neither the embedding model nor Omni was called. This is an
independence failure, not evidence that the proposed top-1/top-2 gate is
effective or ineffective.

See [`verdict.json`](verdict.json) for the machine-readable dependency result.
