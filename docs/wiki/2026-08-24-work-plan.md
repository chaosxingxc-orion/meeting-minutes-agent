# 2026-08-24 工作记录与下一步

## 今日完成

- 完成 Earnings-22 全125场固定 Q8 Sortformer flight：125/125成功，墙钟3.06小时。
- 在工具接触前冻结人数分层、参考对齐门、30场主讲占主导目标组和精度阈值。
- 完成唯一一次参考评分：主假设组Top-1错误14.30%、Top-2错误22.59%，判为
  `MAIN-SPEAKER-DIARIZATION-USABLE`；长尾错误72.75%。
- 归档运行收据、机器结果、正式判读、实验页和脚本；Omni 调用仍为0。

## 阶段判断

固定4-speaker前端不适合“完整分开所有参会人”，但可用于主讲占主导电话会的主要 speaker
路由。后续研究对象可以从“所有 speaker”收缩为“有足量历史和发言占比的 dominant speaker”，
这与用户提出的电话会议结构假设一致。

## 下一决策

是否预注册一个小型 Earnings-22 Omni pilot，只检验 dominant speaker 的专业词转写：

1. 运行时 eligibility 不能使用 gold 发言占比，必须从 Pass-0/diarizer 输出构造。
2. 先做零模型门：预测 cluster 的发言占比能否稳定识别主要主讲，并控制错误路由。
3. 门通过后才冻结少量会议、speaker state 和 `speaker vs global` 对照；不评价长尾 speaker，
   不启动多轮 agent loop。
