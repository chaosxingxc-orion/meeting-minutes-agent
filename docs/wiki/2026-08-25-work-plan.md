# 2026-08-25 工作总结

## 当日结论

8月25日完成了从“官方材料能否提供候选”到“运行时语义门是否存在”的完整零模型证据链。
词法触发和词法路由均未达到安全门；encode-only语义K在原3场结构审计中通过。由于
Earnings-22严格reference-unread会议为0，独立运行时门未执行；降级的construction-isolated
六场复用实验完成材料准入、Pass0、开发阈值拟合和唯一确认读取，最终只支持
`CONSTRUCTION_ISOLATED_SIGNAL_PRESENT`，不支持独立泛化或WER增益结论。

| 实验 | 状态 | 8月25日判决 |
|---|---|---|
| `E-MEETING-MATERIAL-SUPPLY-AUDIT` | 已判读 | 词法触发precision 0.72%、recall 10%，`MEETING-MATERIAL-SUPPLY-INSUFFICIENT` |
| `E-MEETING-MATERIAL-RETRIEVAL-SIGNAL` | 已判读 | 词法归属precision 61.87%，`RETRIEVAL-SIGNAL-INSUFFICIENT` |
| `E-MEETING-MATERIAL-SEMANTIC-SIGNAL` | 已判读 | 语义归属precision 77.86%，3/3场过门，`SEMANTIC-RETRIEVAL-SIGNAL-PRESENT` |
| `E-MATERIAL-SEMANTIC-ADMISSION` | 已判读 | reference-unread会议0/125，严格独立准入失败 |
| `E-MATERIAL-RUNTIME-GATE` | 未运行 | 前置独立准入失败，零Pass0、零embedding、零Omni |
| `E-MATERIAL-RUNTIME-GATE-CI` | 已判读 | 阈值0.01；确认precision 76.10%、覆盖74.82%，`CONSTRUCTION_ISOLATED_SIGNAL_PRESENT` |

## 首要目标

启动 `E-MEETING-MATERIAL-SUPPLY-AUDIT` 的零模型设计，验证与会议同时可得的官方发布稿或演示
材料，能否比单一公司身份提供更丰富、更准确的专业词纠错候选。该阶段不接触Omni，不启动
GRPO、GEPA、EM或多模态策略搜索。

## 执行步骤

1. **冻结来源边界**：仅接受官方issuer/IR域名、明确对应目标会议、发布时间不晚于会议的材料；
   记录URL、发布时间、抓取时间、SHA-256和文件类型。当前外部registry不自动视为M0。
2. **做可得性盘点**：先检查4场冻结会议是否都有release或slides；缺失项和时间证据不足项直接
   标记，不用参考转写补齐。
3. **构建零模型候选**：从材料中确定性抽取公司名、产品/业务名、缩写和字母数字术语；排除纯
   财务常用词、孤立数字和人员名单，并冻结上下文预算与去重规则。
4. **预注册一次性判读**：参考文本只负责事后计算候选precision、纠错机会、跨会议覆盖和
   false-trigger，不参与材料选择、抽取或路由。
5. **按门决策**：供给、精度、分布、泄漏和预算门全部通过后，才讨论来源准入与小型模型实验；
   任一关键门失败即归档该分支，不在已读结果上调阈值。

## 预期产物

- 来源清单与获取回执；
- 预注册、冻结配置和确定性抽取脚本；
- 一次性零模型判读与逐会指标；
- Wiki实验登记、阶段结论和是否放行模型的明确决定。

## 保持禁止

- 不使用gold/reference构造术语表或触发器；
- 不把当前模型输出的重复当作正确性证据；
- 不选择性重听，不改变固定diarizer、切片器和预处理；
- 不因材料可下载就默认其运行时合法性；
- 在稳定性与正确性门通过前，不启动training-free策略优化。

## 今日完成

- 从官方IR/新闻源获取并冻结6个来源记录；3场会议满足候选准入，材料字节保存在E盘。
- 确定性解析77页材料，冻结49个带文档哈希、页码和原文跨度的候选。
- 新增ORG/SUPPLY实现与单元测试；WSL完整离线套件通过：1553 passed、25 skipped。
- 完成唯一一次reference read：979 turns、30个机会、418次触发、3次正确、415次错误；
  precision 0.72%、recall 10%，判为`MEETING-MATERIAL-SUPPLY-INSUFFICIENT`。
- 未调用Omni，未在已读结果上修改候选、别名、阈值或reader。
- 对照伞仓SAEA的+20.20pp结果，迁移其Q-K-V和retain/dispatch结构，新增零模型
  `E-MEETING-MATERIAL-RETRIEVAL-SIGNAL`。729/751个turn可检索，但正确材料归属仅61.87%；
  Galp 77.12%，另两场低于50%，判为`RETRIEVAL-SIGNAL-INSUFFICIENT`。
- 已确认伞仓Qwen3-Embedding-0.6B encode-only工具的固定身份和本机llama-server；模型已下载到
  E盘并通过官方SHA-256校验，synthetic批量API检查为2/2个1024维非零向量。
- 完成`E-MEETING-MATERIAL-SEMANTIC-SIGNAL`：dispatch 393/751，正确材料precision 77.86%，
  相对词法K提高15.997点，3/3场通过逐会门，判为`SEMANTIC-RETRIEVAL-SIGNAL-PRESENT`。
- 明确迁移结论：借鉴伞仓的encode-only语义K与retain/dispatch；不照搬其任务分数，也不把错配
  控制当成部署依赖。下一模型实验必须使用独立reference-unread会议。
- 执行`E-MATERIAL-SEMANTIC-ADMISSION`：v2的80场discovery与v3的45场reserve互不重叠，
  并集覆盖Earnings-22的125/125场，严格reference-unread会议为0，准入失败。
- 按预注册停止`E-MATERIAL-RUNTIME-GATE`：未下载新材料、未检查Pass0、未调用embedding或Omni；
  该状态只说明独立实验面缺失，不说明top1/top2门有效或无效。
- 同事接受较弱证据后，另行注册`E-MATERIAL-RUNTIME-GATE-CI`：冻结Costco、HDFC Bank、
  Sony为开发集，Ferrari、Sanofi、KKR为确认集；明确禁止把该结果写成独立确认。
- 完成六场官方材料准入：冻结4份PDF和2份SEC Exhibit HTML，共54页、1,403个原始候选；
  逐会候选均不少于81，原件/快照哈希齐全，判为`COHORT_MATERIAL_ADMISSION_READY`。
- acquisition阶段保持零reference、零Pass0、零embedding、零Omni；失败下载留下的Ferrari
  0字节占位经精确核验后已删除。
- 单独预注册并完成六场裸`T1-A1` Pass0：固定1,639个Sortformer turn、22,678.133秒音频，
  1,639/1,639成功、0空输出、0重试；结构判决`PASS0_COMPLETE`。该结果只证明baseline供给完整。
- 在embedding接触前冻结语义门实现、模型/服务哈希、622条开发query、850条确认query和最多97次
  批调用；随后按明确授权执行单向开发/确认协议。
- 开发集最低合格阈值冻结为0.01：precision 73.28%、覆盖77.01%、中位余弦优势0.05709。
- 唯一确认读取达到precision 76.10%、覆盖74.82%、中位余弦优势0.06154，2/3场越过逐会60%
  precision门，四项总门全部通过，判为`CONSTRUCTION_ISOLATED_SIGNAL_PRESENT`。
- 本次语义门未调用Omni，也未读取reference；结论不包含独立泛化、WER改善、安全纠错或部署能力。
- 归档后在共享WSL `meeting`环境运行完整离线套件：1555 passed、25 skipped；embedding服务已停止。

## 2026-08-26 TODO

1. 寻找至少6场新会议或独立外部测试集，先确认参考词面从未被当前研究读取，再做材料可得性盘点。
2. 保持六场材料、候选池、Pass0、阈值和确认结果不可覆盖；不得重跑确认门或事后调阈值。
3. 基于已通过的降级零模型门，设计但不擅自运行探索性的`R0-retain / R1-correct-dispatch /
   R2-deranged-dispatch` Omni能力实验；须另行预注册prompt、预算、WER/纠错/误伤门并取得授权。
4. 重点处理确认集逐会异质性，尤其记录58.64%场次的失败模式，但不得在冻结确认集上优化selector。
5. GRPO、GEPA、EM和多模态策略搜索继续不放行；先证明三臂实验中的安全增益，再讨论策略优化。

详细执行顺序、完成标准和授权边界见[2026-08-26工作计划](2026-08-26-work-plan.md)。
