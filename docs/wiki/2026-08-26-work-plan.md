# 2026-08-26 工作总结

## 今日目标

把已通过的construction-isolated语义门转化为可执行、可拒绝的三臂Omni能力实验注册。今天默认
只做设计、零模型预算审计和reader预构建；没有新的明确授权，不接触Omni。

## 必做 TODO

1. **封存8月25日证据**：核对六场材料、候选池、1,639条Pass0、阈值0.01和唯一确认结果的
   哈希与链接；禁止覆盖、重跑确认或在确认集上继续调阈值。
2. **复用既有三臂设计**：以现有[retain/dispatch设计记录](../readiness/2026-08-25-meeting-material-retain-dispatch-next-design.md)
   为起点，明确`R0-retain / R1-correct-dispatch / R2-deranged-dispatch`的完全等剂量输入、候选装配、
   prompt、seed、解码和失败处理，不另造一套协议。
3. **先做零模型预算与功效审计**：冻结eligible turn、确定性抽样、逐会最小支持、调用数、音频秒数、
   token上限和预计墙钟时间。若现有供给不足以识别预注册最小效应，停止模型实验。
4. **预构建一次性reader**：至少判读wrong-to-correct、correct-to-wrong、整体WER、最差speaker WER、
   correct-minus-deranged差异、逐会分布、错误激活和成本；参考只允许在flight完成后由reader读取。
5. **完成预注册后请求授权**：只有配置、runtime manifest、预算、停止规则和read suite全部冻结，才向
   同事申请Omni flight授权；授权前不启动服务。

## 次要 TODO

- 建立外部独立会议候选台账，优先记录音频、参考、同期官方材料和reference-unread状态；该路线用于
  后续泛化确认，不阻塞construction-isolated探索设计。
- 对确认集58.64%场次只做已保存聚合证据的风险登记；不重建逐turn分数、不重读确认集，也不据此
  修改selector。

## 完成标准与继续禁止

- 当日完成标准：形成一份状态为“已注册”的三臂预注册、冻结配置、预算回执和预构建reader测试；
  或由零模型审计给出明确的“不运行”判决。
- 继续禁止：重跑语义确认、事后调阈值、把错配会议分数用于部署选择、使用gold/reference构造prompt、
  未授权Omni调用，以及GRPO、GEPA、EM或多模态策略搜索。

## 需要同事决策

- 同事已选择新surface前瞻持久化trace；旧确认集不再做trace-materialization读取。
- 只有上述缺口补齐并重新通过前置审计，才讨论construction-isolated三臂Omni flight授权。
- 新surface已提升为下一阶段主线；其短片段结论不得外推为完整会议agent-loop稳定性。

## 今日进展

- 已同步`master`到PR #5合入提交并删除两个已合入的本地/远端分支；两个既有本地未跟踪项保持不变。
- 完成零模型预算与可执行性审计：六项冻结输入哈希全部通过，0 embedding、0 Omni、0 reference。
- 聚合预算为R1 636次、R2 636次，共1,272次；模型接触音频因缺turn身份只能界定为
  7,711.296至22,150.248秒。
- 功效规划显示5点效应在10%/20%/30% discordance下需314/628/942对；636个聚合dispatch最多
  支持约20.26%，且不能替代缺失的primary opportunity census。
- 缺冻结逐turn dispatch trace与primary opportunity census，无法构造精确runtime manifest或
  绑定reader，按计划的fail-closed路径判为`NOT_RUN_MISSING_FROZEN_FLIGHT_INPUTS`。
- 新增审计单元测试4项；共享WSL `meeting`环境完整离线套件通过：1559 passed、25 skipped。
- 选择EarningsCallVoice Core-100与FinCall-Surprise的`call_id`连接作为新surface；冻结2019+2020
  的低资源范围，`ECV-0001`因网页预览暴露参考文本而在抽样前排除。
- 新增reference-blind准入配置、逐chunk完整trace schema、append-only持久化与fail-closed验证器；
  raw trace及向量sidecar只存D盘，仓库只保存队列、回执与哈希。
- 100个Core call ID已全部映射到FinCall：2019/2020/2021分别40/30/30。本轮69个候选的138个
  短WAV与69个同场PDF均已取得并通过准入；确定性冻结20开发、40一次确认、9保留，判为
  `NEW_SURFACE_COHORT_FROZEN`。保持0 Pass0、0 embedding、0 Omni、0语义reference读取。
- 新surface准入与trace针对性测试9项通过；共享WSL `~/.venvs/meeting`完整离线套件通过：
  1568 passed、25 skipped。Windows base因缺可选依赖不作为正式测试环境。
- EuphoriaYan已明确授权新surface的实际模型运行。已将第一道模型门单独注册为40-call开发集Pass0：
  592.05音频秒、20,480最大输出token、单slot、零重试，confirmation、reference和材料输入继续封存。
- 已预构建exact-wire launcher与reference-blind结构reader；模型、mmproj和llama-server二进制的
  SHA-256均完成本机复核。完成离线测试后启动唯一Pass0 flight。
- 新surface开发集Pass0已完成并一次性判读：40/40成功、0空输出、0重试，13,797 total tokens，
  全部精确request/response和append-only索引绑定通过，判为`PASS0_TRACE_COMPLETE`。
- Pass0结构reader保持`reference_access=NONE`；该结果不包含WER或能力效果结论。下一步只在开发集
  冻结PDF候选、确定性错配会议和encode-only embedding，confirmation继续封存。
- 开发集材料snapshot通过：20/20场、475页、3,672个候选，按冻结salt选出160个key；最窄一场仍有
  12个候选，超过每场8个的供给门。PDF兼容性警告已保留，但没有缺页或候选失败。
- encode-only flight完成13 batch、200 embedding和40行完整trace；validator对120个向量sidecar及
  Pass0/candidate/row绑定给出`TRACE_COMPLETE`、0错误。
- 开发门判为`DEVELOPMENT_SIGNAL_PRESENT`：正确call归属30/40（75%）、中位优势0.07609、20/20场
  覆盖。冻结最低通过阈值为0.00，因此尚未证明有用的abstention gate；confirmation仍封存。
- EuphoriaYan随后明确放行40场sealed confirmation。确认实验冻结为80次Pass0、1,193.999875音频秒、
  40场PDF、320 key + 80 query及最多25次embedding batch；阈值固定0.00，不在确认集调参。
- confirmation Pass0完成80/80、0空输出、0重试、27,724 total tokens，reference保持未读。
- 后续PDF snapshot在`ECV-0067`/call `2051550`失败：14页PDF仅抽出165字符、0候选<8；按无替换、
  不改extractor、不降width的规则停止，未运行任何confirmation embedding或Omni correction。
- 完成新独立surface零模型检索。首选`LHCP-ASR`：72场人工逐字转写技术报告，原始发布逐场带
  PDF/PPTX，天然25场开发、47场一次确认；报告包含主讲和约5分钟多说话人问答。
- 写入记录前，本仓与伞仓对`LHCP-ASR`和`Chinese-LiPS`均为0命中；本次0语料下载、0模型、
  0 embedding，且未读取LHCP-ASR任何样本reference。
- `LHCP-ASR`暂判`PRIMARY_CANDIDATE_FOUND_SOURCE_ENDPOINT_JOIN_PENDING`：可访问HF镜像的17个评估
  shard共6.25 GiB但只含音频/文本；CERN Indico 2020/2022官方事件已确认逐报告提供slides与
  recording，JSON export可返回稳定附件元数据。下一步先证明72场metadata-only join、覆盖闭合和
  材料可读性供给，不得直接启动Pass0。
- `Chinese-LiPS` validation/test登记为次选；它的slide与人工转写闭合、处理后资源较小，但属于
  专业讲解式单说话人构造，只能作为construction-isolated多模态能力证据。
- 同事明确授权并完成`E-MATERIAL-LHCP-ADMISSION`零模型审计：四个发布split的72条`audio.path`
  全部通过event ID与contribution friendly ID精确对齐，72个contribution唯一，0孤儿、0歧义。
- 72/72场均有同期官方材料，共77个唯一附件（74 PDF、3 PPTX），67场单附件、5场双附件；
  77/77端点通过不读取正文的Range响应头检查，官方声明总大小约1.536 GiB。
- 17个远端long-form shard合计6,705,900,572 bytes，本次列裁剪只传输1,116,237 bytes；保持
  0 reference、0音频正文、0材料正文、0 Pass0、0 embedding、0 Omni。判为
  `LHCP_METADATA_JOIN_AND_MATERIAL_COVERAGE_CLOSED`。
- 下一步必须另行注册材料下载、PDF/PPTX可读性与零模型候选供给审计；准入闭合不等于材料归属
  或WER收益，模型flight继续不放行。
- 同事已授权继续，`E-MATERIAL-LHCP-SUPPLY`完成预注册：固定下载77个附件（1,648,976,797 bytes）
  到D盘，所有附件预先纳入；每场至少1个可读文档、200可见字符和8个候选，72/72全过才放行。
- 77/77附件已下载并完成size、MD5和本地SHA-256校验；D盘材料总量1,648,976,797 bytes，
  未下载任何音频，未读取reference。
- 第一次解析因PDF文本含孤立`U+D835`在写UTF-8 trace时停止，未产出/读取聚合结果；保留部分trace
  后，预先登记仅将非法surrogate替换为`U+FFFD`的修正并使用新目录重跑。
- 最终供给门判为`LHCP_ZERO_MODEL_MATERIAL_SUPPLY_INSUFFICIENT`：70/72场通过，开发25/25、确认
  45/47；两场`test_2020`的单PDF触发`pypdf LimitReachedError`，均为0字符、0候选。
- 其余70场候选最小16、中位142、最大1,205；供给本身很强，但原注册要求72/72，模型flight继续
  不放行。下一步需决策冻结25开发+45确认eligible cohort，或另立全量备用parser实验。
- 同事选择冻结70场eligible cohort。新预注册只依据模型前材料兼容性排除`856696c36.wav`和
  `856696c52.wav`，保持25场开发与45场确认；不修复parser、不换会议，后续结论不得外推到完整72场。
- 70场队列已确定性生成并独立复核：split为14场`dev_2020`、11场`dev_2022`、13场`test_2020`
  和32场`test_2022`；判为`LHCP_70_TALK_ELIGIBLE_COHORT_FROZEN`，validation为
  `TRACE_COMPLETE`且0错误。
- 队列冻结全程保持0 reference、0 Pass0、0 embedding、0 Omni；它不推翻原72/72供给失败，且不
  自动授权模型接触。下一步另行注册25场开发集flight，45场确认在开发选择完成前保持sealed。
- 新增冻结与独立校验定向测试3项通过；冻结后共享WSL完整离线套件通过：1,591 passed、25 skipped。
- 已登记`E-MATERIAL-LHCP-DEVELOPMENT-AUDIO`：只读取25场development的音频列并在D盘保存，禁止
  reference、confirmation和模型接触。发现Indico来源的历史字段`duration_s`实际是议程分钟数，
  因而必须先以解码音频重建秒数预算，不能直接注册Pass0调用量。
- 开发音频已完成：25/25 WAV、2,469,998,494 bytes、37,556.965秒（约10.43小时），逐文件重哈希
  与解码复核为`TRACE_COMPLETE`。23个文件为32 kHz，另有44.1/48 kHz各1个，下一门必须固定
  转为16 kHz mono PCM16后再跑TOOL-LOCKED(B) Sortformer和turn-aware切片。
- 第一次下载在5个WAV后因大Range中断；预先登记16 MiB子Range修正并要求源payload精确匹配后，
  同一冻结队列续跑完成。全程保持0 confirmation、0 reference、0前端模型、0 Omni。
- 固定前端已完成零模型readiness：25个音频、FFmpeg、TOOL-LOCKED(B)二进制/模型、RTTM parser、
  slicer与常量哈希全部通过，判为`FRONTEND_READY_AWAITING_TOOL_AUTHORIZATION`。
- 下一flight冻结为25次Sortformer、37,556.965输入秒、单任务、每场3,600秒、总墙钟2小时；实际
  Sortformer仍待同事明确放行。Pass0预算延后到完整slice manifest，避免事前猜测调用量。
- 本轮新增7项针对性测试；共享WSL完整离线回归通过：1,598 passed、25 skipped。
- 同事明确放行后，固定Sortformer flight完成25/25场、0重试并生成25个非空RTTM；397个切片文件
  与回执全部哈希闭合，结构reader判为`FRONTEND_TRACE_COMPLETE`。
- 最终门控发现15个相邻切片边界重叠，影响10/25场、累计35.900秒、最大14.948秒，违反已注册的
  零重叠硬门，最终判为`FRONTEND_SLICE_ZERO_OVERLAP_GATE_FAILED`。
- 已登记预构建reader漏检缺陷，并以只读slice-bound audit执行原门；0 reference、0 confirmation、
  0 Pass0、0 embedding、0 Omni。不得启动Pass0或重跑Sortformer。

## 2026-08-27 TODO

1. 另行预注册切片器重叠边界修复，明确“重叠turn跨分组”时的唯一边界策略和证据口径。
2. 先补覆盖重叠RTTM的合成单测及全局相邻切片零重叠post-condition，再修改切片器。
3. 获得明确实施授权后，只复用本次25个冻结RTTM重新切片；不重跑Sortformer、不读取reference。
4. 新manifest必须通过零重叠、最长120秒、连续索引、逐文件哈希与25/25会议覆盖门，之后才重新
   计算Pass0精确调用预算并单独申请模型授权。

## 切片器修复进展

- 已预注册`E-MATERIAL-LHCP-SLICER-OVERLAP-FIX`，唯一变化是把重叠连通turn作为原子块打包，并在
  slicer最终出口加入零重叠post-condition；其余输入、参数和模型边界保持冻结。
- attempt-1暴露实现缺陷：代码虽识别原子块，仍逐turn打包，在第24场被post-condition拒绝；23场
  部分产物已封存，无aggregate manifest，也未被复用。
- 修正1改为真正逐原子块打包；新增精确回归fixture后，25场纯规划预检为396片、0重叠、最大120秒，
  重复哈希一致。
- attempt-2完成25/25场并通过预建validator：396片、37,547.256秒、0重叠、最大120秒、0错误；
  普通turn内部切点0，原有单个超长turn例外内部切点4，内部gap为0。
- 全程新增0 Sortformer、0 reference、0 confirmation、0 Pass0、0 embedding、0 Omni。下一步可按
  396次transport调用另行注册开发Pass0，但当前仍未获得模型授权。

## 研究组战略增补

- 研究组决定将总体问题从omni embedding instruction优化扩展为training-free omni agentic system；
  omni embedding降为memory addressor之一，不再承担完整研究故事。
- 新路线先研究memory use，暂时冻结collection/compression/retrieval，并以raw waveform、Pass0 text、
  speaker/time/provenance作为episode-local memory供给。
- 已形成[OmniMinutes proposal](../plans/2026-08-26-omni-agentic-memory-use-proposal.md)，提出
  `no-memory / text / audio / paired / deranged`能力实验和后续agentic modality router。
- 本增补是proposal与调研授权，不是模型flight授权；现有未放行、reference边界和one-shot规则不变。
