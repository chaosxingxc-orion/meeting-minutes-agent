# 2026-08-21 工作计划

## 明日目标

解释 E4-CF 中“speaker 路由方向为正，但只有 +2.16 pp，且出现 109 次 false-hint activation”的机制，形成一个可证伪、可预注册的下一实验方案。明天默认**零模型调用**，不修改 E4-CF 正式判定，不启动 agent loop。

## P0：冻结机制审计口径

在读取分层结果前登记 `E4-CF-MECH` 为探索性审计，冻结以下输出：

- 四臂的 `bare → global/speaker/wrong` 逐 target 变化分类：修复、破坏、不变、诱导插入。
- false-hint activation 的来源：状态词是否出现、来自哪个历史 turn、证据次数、speaker 是否匹配。
- 状态 inventory 长度、证据数、历史距离与收益/污染的关系。
- 以 dialogue 和 speaker 汇总，禁止把 target 当作独立样本重新宣称显著性。

验收物：审计说明、冻结字段/schema、离线测试；明确标注为 post-hoc exploratory。

## P1：实现并运行离线机制审计

新增只读取冻结 binding、responses 和 score manifest 的脚本，不接触模型、不修改任何原始证据。至少回答：

1. speaker 相对 global 的 18 个净增命中来自哪些变化类型；
2. 109 次 speaker false-hint activation 中，多少伴随真实修复、多少是无害插入、多少造成 reference 错误；
3. wrong-speaker 的 70 次激活为何没有造成更高总体 WER；
4. 是否存在仅依赖运行时可见信息的简单安全门，例如最小证据数或 inventory cap，而不是 gold/error selector。

验收物：机器可读结果、中文报告、测试结果和证据哈希；不得覆盖 E4-CF 的一次性正式 read。

## P2：形成下一实验决策

根据机制审计只选择以下一种结论：

- `NO-ACTIONABLE-MECHANISM`：没有合法、稳定的运行时特征，停止继续扩展 speaker state；
- `PREREGISTER-ONE-FIXED-POLICY`：只提出一个固定策略修改，例如 evidence gate 或更小 inventory cap；
- `SAFETY-RISK-DOMINATES`：false-hint 风险过高，优先研究供给拒绝而非转写增益。

若选择第二项，只起草新实验预注册，冻结独立样本、主效应、安全门、预算和 render hash；**未经再次授权不进行模型接触**。

## 明日明确不做

- 不重跑 Z 系列，不优化 diarizer、切片器或预处理。
- 不对 E4-CF 的 5 pp 门槛做事后修改。
- 不按 gold 错误挑选片段，不做选择性重听。
- 不启动 GEPA、GRPO、EM 或多轮 agent loop。
- 不用探索性分层结果替换 `DIRECTIONAL-NOT-CONFIRMED`。

## 汇报检查点

- 上午：完成审计口径、schema 与测试设计。
- 下午：完成离线审计和机器结果。
- 收尾：更新 Wiki、给出三选一决策，并说明是否值得起草下一次预注册。

## 执行结果

- P0 完成：审计设计、输入、实现 hash 和三选一规则已在真实读取前冻结。
- P1 完成：774/774 状态重建一致；正式读取零模型完成；联合回归测试 7/7 通过。
- P2 完成：机器决策为 `PREREGISTER-ONE-FIXED-POLICY`，唯一入选 `speaker_wrong_disjoint`。
- 已起草固定策略：speaker/wrong inventory 不重叠时使用 speaker，否则回退 global。
- 下一步仅做未见数据的零模型 power/roster audit；尚未授权任何新模型调用。

关联材料：[今日进展总结](2026-08-20-progress-summary.md)、[E4-CF 正式判读](../readiness/2026-08-20-e4-confirmatory-verdict.md)、[完整研究计划](../plans/2026-08-20-speaker-conditioned-transcription-optimization.md)。
