# 2026-08-28 工作总结与 2026-08-29 计划

## 今日目标

在 E 盘未接入的条件下，闭合 LHCP-ASR 25 场开发集的 Pass0、材料检索供给和纠错机会验证；所有实验遵守“先注册、再授权、单次 flight、一次性 reader”流程，45 场确认集继续封存。

## 今日完成

1. **完成开发集 Pass0**：冻结 25 场、396 个零重叠切片及 Omni 请求协议。唯一 flight 获得 396/396 响应、0 空输出、0 重试，耗时 4 小时 16 分，使用 641,206 tokens；position 301 因长度上限结束，已标记且禁止选择性重听。
2. **验证会议级语义归属**：从 4,886 个材料候选中冻结 200 个 key 和 396 条因果 query。38 次 embedding 请求产生 596 个向量，正确会议胜出 359/396（90.66%），25/25 场覆盖，中位余弦优势 0.11701。
3. **获授权读取开发 reference 并做功效审计**：仅投影 25 场开发集的 `audio.path + transcription`，传输 666,630 bytes；确认集、测试集和音频正文读取均为 0。Pass0 描述性 micro-WER 为 14.81%。
4. **定位纠错供给瓶颈**：原冻结 top-1 只有 12 个纠错机会；8-key 候选池只有 39/396 个机会片，而完整 4,886 候选池可支持 206/396 个机会片并覆盖 25/25 场。材料源足够，局部候选抽取不足。
5. **比较两类 reference-blind 抽取器**：BM25 `current+prior` top-8 命中 47/206；唯一 331-call 全池语义 flight 完成 5,282 embeddings、0 重试，top-8 命中 53/206（25.73%），仅比 BM25 多 6 个，仍远低于 157 个主门。
6. **完成工程闭环**：新增完整配置、readiness、launcher、reader、trace 校验及实验页。WSL 离线套件最终为 **1,629 passed、25 skipped**，`git diff --check` 通过，所有模型服务已关闭。

## 关键实验结论

| 层级 | 结果 | 判定 |
| --- | ---: | --- |
| 会议归属 | 359/396，90.66% | 会议级语义信号存在 |
| 原 top-1 供给 | 12 个机会片 | 功效不足 |
| 冻结 8-key 池上限 | 39/396 | 候选池不足 |
| 完整材料池上限 | 206/396 | 材料源具备功效 |
| BM25 top-8 | 47/206 | 未过探索门 |
| 语义 top-8 | 53/206 | 仅探索性、未过主门 |

阶段性结论是：**高会议归属准确率不等于可用的专业词纠错供给**。当前材料库已经包含足够多的正确候选，但按整段文本做词法或通用语义检索，无法稳定把相关局部跨度排进 top-8。瓶颈已从“有没有材料”收敛为“如何生成和定位局部候选”。因此不启动 1,188-call Omni correction，也不解封确认集。

## 2026-08-29 计划

1. 封存并复核今日 receipt、manifest、reader 结果与 SHA-256，整理提交和 PR；不提交音频、reference、向量或外部完整 trace。
2. 对 206 个开发机会片做**零模型、描述性**漏检分类，区分缩写/别名、同音或近音、跨页分散、query 缺少锚点和材料粒度过粗；不得据此在开发集事后选择最终阈值。
3. 分别预注册两个候选生成方向：`phonetic/alias normalization` 与 `page/span-level localization`。先冻结输入、预算、错配控制、top-k 门和 reader，再决定是否复用现有 embedding。
4. 优先使用离线规则和冻结向量验证供给上限；只有新 extractor 达到预注册门，才申请新的模型 flight。
5. 45 场确认集继续 sealed；不得启动 confirmation Pass0、reference 读取、Omni correction、GRPO、GEPA 或 EM。

## 当前状态

`E-MATERIAL-LHCP-FULL-POOL-SEMANTIC-EXTRACTOR` 判为 `FULL_POOL_SEMANTIC_EXTRACTION_EXPLORATORY_ONLY`。今日模型调用均已结束，server 端口关闭；下一阶段仍处于开发集候选抽取研究，而非系统级增益确认。
