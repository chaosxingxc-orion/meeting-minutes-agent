# 2026-08-27 工作计划

## 今日目标

基于8月26日闭合的396个零重叠LHCP-ASR开发切片，完成reference-blind开发Pass0的零模型注册、
精确预算、launcher和reader预构建。默认不接触模型；只有所有输入、预算和停止规则冻结后，才
请求唯一development flight授权。

## 必做 TODO

1. **同步研究检查点**：等待PR #6合入后同步`master`并清理已合入分支；若PR仍在审查，只处理
   必要修正，不在同一分支追加模型flight。
2. **注册开发Pass0**：以slice manifest SHA-256
   `1224f0951c6b255523197974368c54e73fd27c4a9b328bf5c909eaf226d695ce`冻结25场、396片、
   37,547.256音频秒及manifest顺序。确认队列和reference继续sealed。
3. **冻结模型与请求协议**：记录Omni模型、mmproj、server二进制、prompt、decode、最大输出token、
   失败处理、并发数和超时；每个transport slice最多一次调用，不允许选择性重听或参数重试。
4. **预建exact-wire launcher**：逐slice保存request、response、speaker/time/provenance、token、
   wall time和append-only receipt；外部trace写D盘，仓库只保存配置、回执、判决与哈希。
5. **预建reference-blind reader**：只检查396/396响应、空输出、失败、重试、输入输出哈希和成本；
   不读取reference，不计算WER，不做材料候选或策略选择。
6. **零模型readiness**：复核396个WAV、最大120秒、模型/服务哈希、D盘空间、token上限和预计墙钟；
   任一门失败即停止。readiness通过后再向同事请求明确Pass0授权。

## 条件性后续

若开发Pass0之后得到396/396完整trace，只放行下一项零模型工作：冻结25场同期材料候选、确定性
错配映射和逐slice查询供给。material retrieval、embedding、Omni correction以及
`text/audio/paired/deranged` memory-use实验都必须另行注册和授权，不能追加到Pass0 flight。

## 当日完成标准

- 形成状态为“已注册”或“readiness未通过”的`E-MATERIAL-LHCP-DEVELOPMENT-PASS0`记录；
- runtime manifest、预算、launcher、reader及测试全部哈希冻结；
- 若未取得明确模型授权，当日模型调用保持0；
- Wiki总表、阶段结论和readiness回执同步更新。

## 继续禁止

- 不读取LHCP-ASR reference、45场confirmation音频或确认材料效果；
- 不重跑Sortformer，不覆盖397片失败证据或396片修复manifest；
- 不把切片完整性、材料可读性或Pass0供给改写为WER或专业词增益；
- 未经新授权不启动Pass0、embedding、Omni、GRPO、GEPA或EM。
