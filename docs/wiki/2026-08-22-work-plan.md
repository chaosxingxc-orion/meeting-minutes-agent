# 2026-08-22 当前工作计划

## 今日目标

确认 Earnings-22 是否真正提供 speaker-exclusive 的专业/技术词供给，而不是仅有广义 WER 标签复现。默认保持零模型、无音频；45 场 reserve 是否启用是首要决策门。

## P0：reserve 使用决策

由负责人确认是否允许将45场未读 reserve 用于一次独立窄类供给审计。

- 若不允许：停止 Earnings-22 路线，reserve 继续封存；转向 Academic/ICSI 域内设计或寻找第三个 business corpus。
- 若允许：先提交新预注册，再读取 reserve；不得回看或重算 discovery 来选择阈值。

## P1：冻结窄类确认设计

候选 ID：`E4-XDOMAIN-SUPPLY-AUDIT-v3`。唯一纳入的上游类别为 `ABBREVIATION` 与 `ALPHANUMERIC`，因为它们与 v1 的严格缩写/字母数字代理最接近。固定使用现有90秒 pseudo-slice、speaker-exclusive carry 定义和 manifest 中的45场 reserve。

预注册必须写清：meeting-level eligibility、总 carry、单 surface 集中度门槛，失败/通过判决和唯一读取规则。`CONTRACTION`、`FALLBACK`、`RANGE`、`PHONE`、`WEBSITE` 不进入主要指标；不得在读取后增删类别。

## P2：实现与预读验证

只新增 reserve-only read suite 和合成测试，验证：

1. discovery 文件被强制拒绝；
2. 输出不包含 surface、实体 ID 或逐会议记录；
3. 输入 manifest、代码 hash、阈值和输出目录覆盖保护全部 fail closed；
4. 完整离线测试通过后再登记实现 amendment。

## P3：条件执行与判读

只有 P0 明确授权且 P1/P2 已提交后，才执行一次 reserve 聚合读取。

- 通过：只说明 Earnings-22 窄类供给可行，下一步进入音频许可与最小 acquisition 设计；不自动授权下载或模型调用。
- 失败：停止 Earnings-22，不放宽类别或阈值；保留 Academic/ICSI 域内路线作为独立备选。
- 完整性异常：判为 `INVALID-AUDIT`，记录失败，不静默重跑。

## 今日明确不做

- 不下载 Earnings-22 音频，不联系模型，不运行第二遍转写。
- 不重跑 Z 系列、E4-CF 或已否决的 safety gate。
- 不用 discovery 结果调 reserve 阈值，不把广义标签机械通过写成专业实体确认。
- 不启动 E5、GEPA、GRPO、EM 或多轮 agent loop。

## 今日验收物

- reserve 使用决定；
- 若放行：v3 预注册、实现/manifest hash、离线测试、唯一机器 verdict；
- Wiki 实验页、阶段性结论和下一决策同步更新。

## 执行结果

- P0 已授权：允许对45场未读 reserve 做一次窄类零模型审计。
- P1/P2 已完成：类别固定为 `ABBREVIATION/ALPHANUMERIC`，reserve-only manifest、reader、哈希和测试在读取前提交。
- P3 已完成：30/45场 eligible、264个 speaker-exclusive carry、最大单 surface 11.36%，判为 `EARNINGS22-NARROW-SUPPLY-FEASIBLE`。
- discovery 读取0、模型调用0、音频使用0。下一决策转为音频许可与最小 acquisition；模型 pilot 仍未授权。

## 后续授权与音频入库

- 负责人后续明确授权下载音频；原计划中的“不得下载”限制因此被该项新授权取代，模型调用禁令仍保持。
- 已获取官方固定版本125个 MP3，共1,908,056,329字节，逐文件 LFS SHA-256 全部通过，`.part` 为0。
- 入库审计的哈希、ID 联结和解码门通过；预注册 CSV 时长门失败，正式判为 `EARNINGS22-AUDIO-NOT-ADMITTED`。
- 参考诊断显示116/125场超过锁定前端4-speaker上限；兼容子集仅9场，暂不启动模型 pilot。
- 下一决策：停止 Earnings-22 固定前端路线，或另行注册不限人数前端 smoke。不得用 gold speaker 数做逐会议运行时路由。
